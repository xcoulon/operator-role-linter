package main

// this is a utility to connect to an OpenShift cluster to download the API Resources in a YAML format
// next, run `make download generate`, having valid $(CLUSTER_URL) and $(TOKEN) env vars
